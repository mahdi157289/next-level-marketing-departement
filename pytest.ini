[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

markers =
    live: hits real network / LM Studio; requires RUN_LIVE_TESTS=1 and prerequisites (see docs)
