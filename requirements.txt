# Phase B — API + database (agent/ML stack added in later phases)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
alembic>=1.13.0
pydantic>=2.6.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
httpx>=0.27.0
openai>=1.40.0
jinja2>=3.1.0
python-multipart>=0.0.9

# Tests
pytest>=8.1.0
pytest-asyncio>=0.23.0

# Later: crewai, litellm, faiss-cpu, sentence-transformers, playwright, celery, redis, etc.
# See doc7_environment_config.md
# Web search: prefer `ddgs` (duckduckgo_search was renamed; see terminal warning).
# ddgs 9.9+ requires Python 3.10+; keep 9.8.x on Python 3.9.
ddgs>=9.0.0,<9.9.0
duckduckgo-search>=5.3.0

# Phase C — SEO + scrape
beautifulsoup4>=4.12.0
playwright>=1.40.0
